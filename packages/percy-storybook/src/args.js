export const docs = 'Documentation: https://github.com/percy/react-percy';

export const options = {
    config: {
        alias: 'c',
        description: 'Path to the storybook config file',
        requiresArg: true
    }
};

export const usage = 'Usage: $0 --config=<storybookConfigPath>';
